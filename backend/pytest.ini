[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
markers =
    slow: marks tests that require a real Claude API key (deselect with -m "not slow")
